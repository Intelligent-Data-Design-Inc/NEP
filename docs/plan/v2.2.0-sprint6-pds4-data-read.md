# V2.2.0 Sprint 6: Read PDS4 Data

**Objective**: Implement variable data reading for PDS4 arrays and tables so that `nc_get_vara()` returns correct values from the referenced data files.

---

## Background

PDS4 labels reference separate data files. So far the metadata model is complete. This sprint adds the actual I/O:
- Array data: read raw bytes from the data file and convert endianness/type as needed.
- Table data: read fixed-record binary/ASCII records or parse delimited lines.

The dispatch entry point is `NC_PDS4_get_vara()` in `src/pds4file.c`.

---

## Matrix

| Dimension | Values |
|-----------|--------|
| `build_system` | `cmake`, `autotools` |
| Format handlers | PDS4 ON, all others OFF |
| Compression | OFF |
| Parallel tests | OFF |

2 jobs total.

---

## Tasks

### 0. Store data-layout metadata during label parse

Sprint 5 builds the netCDF metadata model (dimensions, variables, types) but does
not retain the byte offsets and lengths needed to locate data within the binary
file. Before data reading can work, the label-parse pass must record:

| Metadata | Source element | Storage location |
|---|---|---|
| Array byte offset in data file | `Array/offset` | `var->format_var_info` |
| Table byte offset in data file | `Table_*/offset` | group-level or `var->format_var_info` |
| Record length (bytes) | `Record_*/record_length` | `var->format_var_info` |
| Field byte offset within record | `Field_*/field_location` (1-based) | `var->format_var_info` |
| Field byte length | `Field_*/field_length` or `Field_*/maximum_field_length` | `var->format_var_info` |

Implementation approach:
- Define a small `NC_PDS4_VAR_INFO_T` struct (file offset, record length, field
  offset, field length) and attach it to `var->format_var_info` during
  `pds4_read_array()` and `pds4_read_table()`.
- For arrays, also store the total data offset so `get_vara` can seek directly.
- Free the struct in `NC_PDS4_close()` by walking all variables.

### 1. Resolve data file paths

Given the label path and the `File/file_name` stored in the file-area group state, compute an absolute or relative path to the data file in the same directory as the label. Use POSIX path helpers already present in the codebase if available.

### 2. Implement array data reading

For `Array` / `Array_2D_Image` variables:
- Open the resolved data file.
- Map netCDF 0-based `start[]/count[]` to file offsets using the dimension lengths and element size.
- Read the requested hyperslab.
- If the label `data_type` is MSB and the host is little-endian (or vice versa), byte-swap each element.
- Convert from the file type to the requested `memtype` if they differ.
- Support contiguous arrays; chunked or specialized PDS4 storage can be deferred.

### 3. Implement table field data reading

For fixed-record binary/character tables:
- Use the per-field offset and length stored in `var->format_var_info` (from Task 0).
- For a row range `start[0]..start[0]+count[0]-1`, seek to `table_offset + record_length * row + field_offset` for each record and read `field_length` bytes.
- For ASCII numeric fields (`ASCII_Real`, `ASCII_Integer`), parse the text substring into the mapped netCDF type (strtod/strtoll).
- For binary fields, byte-swap if endianness differs from host.

For delimited tables:
- Scan the file to the requested rows, split the line by the delimiter, and convert the appropriate column.

### 4. Update tests with data assertions

Extend `test/tst_pds4_udf.c`:
- Read a small hyperslab from `test_image.img` and verify at least one known pixel value.
- Read one or more rows/fields from `test_table.*` and verify expected values.

Add the known values as constants in the test; extract them from the sample files before implementation.

### 5. Remove metadata/data limitations from docs

- Update `docs/prd.md` section 14.5 to remove the "no metadata/data reading" limitation.
- Update `README.md` to describe that PDS4 files can be opened and read via the NetCDF API.
- Update `.devin/skills/pds4/SKILL.md` with data-read notes (endianness, path resolution).

### 6. Verify CI

Ensure the existing PDS4-only job in `ci-formats.yml` continues to pass and now exercises data reads.

---

## Definition of Done

- [ ] `nc_get_vara()` returns correct data for the `test_image.img` array sample.
- [ ] `nc_get_vara()` returns correct data for the table sample.
- [ ] Byte order is honored for MSB binary types.
- [ ] Data file paths are resolved relative to the XML label directory.
- [ ] Tests verify array and table data values.
- [ ] PRD, README, and skill file no longer state that PDS4 reading is unsupported.

---

## Dependencies Between Sprints

- **Sprint 4**: Provides array dimensions, variables, and types.
- **Sprint 5**: Provides table dimensions, variables, and types. Does NOT store
  byte offsets or field lengths needed for I/O — Task 0 above fills that gap.

---

## Notes

- PDS4 `Array` data is stored in the byte order declared by its `data_type` (MSB/LSB). The reader must swap when host byte order differs.
- `nc_get_vara()` is the primary target; `nc_get_vars()` and `nc_get_varm()` can rely on the default dispatch fallbacks (`NCDEFAULT_get_vars`, `NCDEFAULT_get_varm`) until performance requires otherwise.
- Keep the reader read-only; write paths remain disabled.
