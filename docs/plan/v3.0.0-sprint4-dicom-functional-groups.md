# V3.0.0 Sprint 3: Functional Groups, Fortran Bindings, Documentation, and CI

**Objective**: Complete the DICOM reader by exposing per-frame and shared functional-group metadata as NetCDF variables/attributes, add a Fortran smoke test, update all user-facing documentation and build integration, and wire the reader into the existing CI matrix so DICOM files can be opened through the NetCDF API from both C and Fortran.

---

## Background

Sprints 1 and 2 provide native and encapsulated pixel-data access for single-frame and multi-frame DICOM images. Many clinical DICOM objects — especially CT/MR Enhanced, PET, and digital mammography — store 3D or 4D volumes as multi-frame instances where each frame's geometry, timing, and acquisition parameters live in **Functional Groups** rather than top-level tags.

This sprint:

- Parses **Shared Functional Groups Sequence** `(5200,9229)` and **Per-frame Functional Groups Sequence** `(5200,9230)`.
- Exposes selected frame-level metadata as 1-D NetCDF variables over the `frame` dimension.
- Adds a Fortran test using `nf90_open` after calling `NC_DICOM_initialize()`.
- Updates documentation (formats page, PRD, design, README) and Conda/Spack recipes to include DICOM.
- Integrates DICOM into the existing `ci.yml` matrix and adds a DICOM example program.

---

## Matrix

| Dimension | Values |
|-----------|--------|
| `build_system` | `cmake`, `autotools` |
| `language` | C, Fortran |
| `metadata` | shared + per-frame functional groups |
| `documentation` | README, formats page, PRD, design |
| `packaging` | Spack, Conda |
| `ci` | `ci.yml` + `ci-dicom.yml` |

---

## Tasks

### 1. Parse Functional Group Sequences

For multi-frame enhanced objects, locate:

- **Shared Functional Groups Sequence** `(5200,9229)` — one item containing attributes shared by all frames.
- **Per-frame Functional Groups Sequence** `(5200,9230)` — one item per frame.

Each item is a `DcmDataSet` (or `DcmSequence` in `libdicom` terms) nested inside the parent data set. Recursively retrieve elements from:

```
(5200,9229) SQ
  Item 1
    (0020,9113) Plane Position Sequence
      (0020,0032) ImagePositionPatient
    (0020,9116) Plane Orientation Sequence
      (0020,0037) ImageOrientationPatient
    (0028,9110) Pixel Measures Sequence
      (0018,0050) SliceThickness
      (0028,0030) PixelSpacing
(5200,9230) SQ
  Item 1..N
    (0008,0012) InstanceCreationDate
    (0008,0013) InstanceCreationTime
    (0008,9123) FrameContentSequence
      (0008,1160) FrameReferenceDateTime
```

For this sprint, expose at minimum:

| Tag / nested path | Variable or attribute | Shape |
|-------------------|-----------------------|-------|
| `ImagePositionPatient` (0020,0032) | `image_position_patient` | `[frame][3]` or global if in Shared FG |
| `ImageOrientationPatient` (0020,0037) | `image_orientation_patient` | `[frame][6]` or global if in Shared FG |
| `PixelSpacing` (0028,0030) | `pixel_spacing` | `[frame][2]` or global |
| `SliceThickness` (0018,0050) | `slice_thickness` | `[frame]` or scalar attribute |

If a value is in the Shared Functional Groups Sequence, create a scalar global attribute or a constant variable. If it varies per frame, create a `[frame]` variable.

### 2. Expose functional-group variables

When `NumberOfFrames > 1` and functional groups are present:

- Add a `frame` dimension.
- Add `position` dimension of length 3 for `image_position_patient`.
- Add `orientation` dimension of length 6 for `image_orientation_patient`.
- Add `spacing` dimension of length 2 for `pixel_spacing`.
- Create variables of type `NC_DOUBLE` for pixel spacing, slice thickness, image position, and image orientation.

These variables are read-only. `NC_DICOM_get_vara()` returns the corresponding slice of the parsed arrays stored in `NC_DICOM_FILE_INFO_T`.

### 3. Add Fortran test `ftest/ftst_dicom_udf.F90`

Mirror `ftest/ftst_fits_udf.F90`:

- Call `NC_DICOM_initialize()` (C binding).
- Open the uncompressed DICOM test file with `nf90_open(..., NF90_NOWRITE, ...)`.
- Verify dimensions and a few attributes.
- Read a small slice of `pixel_data`.
- Close the file and print `Done.`.

Update `ftest/CMakeLists.txt` and `ftest/Makefile.am` to build the Fortran test when `HAVE_DICOM` and `HAVE_FORTRAN` are true. Link against `libncdicom` (the DICOM dispatch object library) or `libnep` if the DICOM objects are folded into the main library.

### 4. Add a C example program

Create `examples/dicom/dicom_read.c` (and optionally `examples/dicom/f_dicom_read.f90`):

- Open a DICOM file via `nc_open` after `NC_DICOM_initialize()`.
- Print compact metadata: PatientName, Modality, Rows, Columns, NumberOfFrames, SamplesPerPixel, BitsAllocated, PhotometricInterpretation.
- Read and print a small corner of `pixel_data`.
- Follow the NEP example style (`ERR(retval)` macro, compact output, `Done.`).
- Add `@note` referencing the NetCDF Developer's Handbook Second Edition.

Add `examples/dicom/CMakeLists.txt` and `Makefile.am`, conditionally included under `HAVE_DICOM`.

### 5. Update documentation

#### `docs/formats.md` and new `docs/dicom.md`

- Create `docs/dicom.md` with an overview, data model, supported transfer syntaxes, build instructions, and an example.
- Update `docs/formats.md` to link to `docs/dicom.md` and list DICOM in the supported-formats table.

#### `docs/design.md`

- Add DICOM to the UDF slot allocation table (UDF6).
- Add `libdicom` to the format-libraries list.
- Update the architecture diagram/table to include `[DICOM Handler]`.

#### `docs/prd.md`

- Add a new section `## X. DICOM Format Support (v3.0.0)`.
- Document features, UDF slot, build configuration, dependencies, limitations, and known issues.

#### `README.md`

- Add DICOM row to the format-reader table.
- Update quick-reference option list with `-DNEP_ENABLE_DICOM` / `--enable-dicom` defaulting to OFF.

### 6. Update packaging

#### Spack (`spack/NEP/package.py`)

- Add a `+dicom` variant.
- Add `depends_on('libdicom', when='+dicom')`.
- Add `depends_on('libjpeg-turbo', when='+dicom')` or `depends_on('libjpeg', when='+dicom')`.
- Pass `-DNEP_ENABLE_DICOM=ON` when `+dicom`.

#### Conda (`conda/meta.yaml`)

- Add `libdicom` and `libjpeg-turbo` host dependencies.
- Enable DICOM in the build script.

### 7. Integrate DICOM into CI

#### `ci-dicom.yml`

- Keep this workflow as the focused DICOM build.
- Add matrix entries for libjpeg-turbo and multi-frame sample files if available.

#### `ci.yml`

- Add `-DNEP_ENABLE_DICOM=ON` to at least one matrix job (e.g., the full-featured CMake job).
- Install `libdicom-dev` and `libjpeg-turbo8-dev` via `apt-get`.
- Cache the existing dependency keys; no new caches are required if libdicom is installed from apt.

### 8. Add more Transfer Syntax support (optional stretch)

If time permits and dependencies are available:

- **RLE Lossless** (`1.2.840.10008.1.2.5`) — implement a small RLE decoder or use an existing C library.
- **JPEG Lossless** (`1.2.840.10008.1.2.4.57`, `1.2.840.10008.1.2.4.70`) — extend libjpeg usage or document as unsupported.
- **JPEG 2000** (`1.2.840.10008.1.2.4.90`, `1.2.840.10008.1.2.4.91`) — requires `openjpeg`; defer if not straightforward.

Document whichever Transfer Syntaxes are explicitly supported or rejected.

### 9. Robustness and edge cases

- Reject DICOM files without a valid `DICM` prefix at offset 128 unless the user passes `NC_UDF6` explicitly (some ACR-NEMA files omit the prefix).
- Handle missing `NumberOfFrames` as `1`.
- Handle missing `SamplesPerPixel` as `1`.
- Handle missing `PlanarConfiguration` as `0` for RGB images.
- Ensure graceful failure when `libdicom` returns `NULL` metadata or frame data.

---

## Implementation Details

### Functional-group data structures

Extend `NC_DICOM_FILE_INFO_T`:

```c
typedef struct NC_DICOM_FILE_INFO {
    DcmFilehandle *filehandle;
    char *path;
    int transfer_syntax;
    int nframes;
    size_t rows;
    size_t columns;
    int samples_per_pixel;
    int bits_allocated;
    int pixel_representation;
    int planar_configuration;
    int encapsulated;

    /* Functional group arrays, allocated when nframes > 1 */
    double *image_position;     /* [nframes][3] */
    double *image_orientation;  /* [nframes][6] */
    double *pixel_spacing;      /* [nframes][2] */
    double *slice_thickness;    /* [nframes] */
} NC_DICOM_FILE_INFO_T;
```

Populate these arrays during `NC_DICOM_open()` by walking the Shared and Per-frame Functional Group Sequences. Free in `NC_DICOM_close()`.

### Variable dispatch in `NC_DICOM_get_vara()`

- `pixel_data` → pixel read path.
- `image_position_patient`, `image_orientation_patient`, `pixel_spacing`, `slice_thickness` → copy from the pre-parsed arrays.
- Any other variable name → `NC_ENOTVAR`.

### Fortran interface

```fortran
interface
  function NC_DICOM_initialize() bind(C, name="NC_DICOM_initialize")
    use iso_c_binding
    type(c_ptr) :: NC_DICOM_initialize
  end function NC_DICOM_initialize
end interface
```

---

## Acceptance Criteria

- Functional-group metadata variables are created for multi-frame enhanced DICOM files when present.
- `ftest/ftst_dicom_udf.F90` compiles and passes under both build systems.
- `examples/dicom/dicom_read.c` builds, runs, and produces compact output ending with `Done.`.
- `docs/dicom.md`, `docs/formats.md`, `docs/design.md`, `docs/prd.md`, and `README.md` all describe DICOM support accurately.
- Spack and Conda recipes include an optional `+dicom` / DICOM-enabled build.
- `ci.yml` builds and tests DICOM in at least one job.
- All DICOM tests pass with DICOM enabled, and existing tests continue to pass with DICOM disabled.
- Unsupported Transfer Syntaxes and malformed files fail cleanly.

---

## Testing

- Run `ctest -R dicom --output-on-failure` and the Autotools `make check` DICOM subset.
- Run the Fortran test `ftst_dicom_udf`.
- Run the C example `dicom_read`.
- Test a multi-frame enhanced DICOM sample (if available) for functional-group variable creation.
- Verify `ci-dicom.yml` and the DICOM-enabled `ci.yml` job both pass.
- Run a full test suite with DICOM disabled to confirm no regressions.

---

## Build System Integration

- `CMakeLists.txt`: keep `NEP_ENABLE_DICOM` option; add `examples/dicom/CMakeLists.txt` conditional.
- `examples/CMakeLists.txt`: add `add_subdirectory(dicom)` under `HAVE_DICOM`.
- `configure.ac` / `examples/Makefile.am`: equivalent conditional inclusion.
- `ftest/CMakeLists.txt` and `ftest/Makefile.am`: add `ftst_dicom_udf` target under `HAVE_DICOM`.
- `docs/Makefile.am` / Doxygen: include `docs/dicom.md` and any new source files.
- `spack/NEP/package.py`: add `+dicom` variant and dependencies.
- `conda/meta.yaml` and `conda/build.sh`: add `libdicom` and libjpeg host dependencies; enable DICOM.

---

## Definition of Done

The DICOM reader supports functional-group metadata, has a Fortran smoke test and a C example, is documented across all user-facing docs, is wired into Spack/Conda/CI, and the full test suite passes with DICOM enabled while remaining regression-free with DICOM disabled.

**GitHub Issue:** TBD
