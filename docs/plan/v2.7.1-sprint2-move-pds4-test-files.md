# v2.7.1 Sprint 2: Move Some PDS4 Test Files

## Sprint Goal and Scope

Reorganize the PDS4 test-data tree so mission-specific and generic test files live in clearly named subdirectories, and update the build systems and tests to find the files in their new locations.

No PDS4 reader changes are in scope.

## Files to Move

Move the following files in the source tree (`test/data/PDS4/`):

- All `mvn_*` files → `test/data/PDS4/maven/`
  - `mvn_ngi_l1b_cal-hk-058943_20250101T023235_v01_r02.xml`
  - `mvn_ngi_l1b_cal-hk-058943_20250101T023235_v01_r02.csv`
  - `mvn_ngi_l3_res-sht-58942_20250101T010116_v06_r03.xml`
  - `mvn_ngi_l3_res-sht-58942_20250101T010116_v06_r03.csv`
  - `mvn_iuv_l2_corona-orbit00407-fuv_20141214T192758.xml`
  - `mvn_iuv_l2_corona-orbit00407-fuv_20141214T192758_v07_r01.fits`
  - `mvn_iuv_l2_periapse-orbit00124_20141021T132108.xml`
  - `mvn_iuv_l2_periapse-orbit00124_20141021T132108_v13_r01.fits`

- All `ZLF_*` files → `test/data/PDS4/perseverance/`
  - `ZLF_1738_0821212185_707RAD_N0830000ZCAM00091_1100LMJ01.xml`
  - `ZLF_1738_0821212185_707RAD_N0830000ZCAM00091_1100LMJ01.IMG`
  - `ZLF_1737_0821123689_910RAD_N0830000ZCAM00091_1100LMJ01.xml`
  - `ZLF_1737_0821123689_910RAD_N0830000ZCAM00091_1100LMJ01.IMG`

- All `Table_*` and `test_*` files → `test/data/PDS4/general/`
  - `Table_Character_Example.xml`
  - `Table_Character_Example.tab`
  - `test_image.xml`
  - `test_image.img`
  - `test_table_binary.xml`
  - `test_table_binary.dat`

Existing subdirectories `cassini_hrd/`, `lcs_9p/`, `messenger_tnmap/`, and `new_horizons/` are left unchanged.

## Build System Updates

### CMake (`test/CMakeLists.txt`)

- Add `file(MAKE_DIRECTORY ...)` calls for `data/PDS4/maven`, `data/PDS4/perseverance`, and `data/PDS4/general`.
- Update all `configure_file()` calls for moved files to use the new source and destination paths.
- Remove any now-unused `configure_file()` calls for the old top-level paths.

### Autotools (`test/Makefile.am`)

- Update the `check-local` copy commands to create the new subdirectories and copy files to the new locations.
- Update `EXTRA_DIST` to list the files under their new subdirectory paths.

## Test Updates

- Update the affected `#define PDS4_*_FILE` path constants in `test/tst_pds4_udf.c` to reflect the new subdirectory locations.
- Affected constants include at least:
  - `PDS4_TEST_FILE`
  - `PDS4_TABLE_BINARY_FILE`
  - `PDS4_TABLE_CHAR_FILE`
  - `PDS4_MAVEN_L1B_FILE`
  - `PDS4_MAVEN_L3_FILE`
  - `PDS4_MAVEN_IUVS_FILE`
  - `PDS4_MAVEN_PERIAPSE_FILE`
  - `PDS4_PERSEVERANCE_FILE`
  - `PDS4_PERSEVERANCE_1737_FILE`

## Documentation Updates

- Add a brief note to `docs/pds4.md` describing the test-data directory layout under `test/data/PDS4/`.
- Do not rewrite existing mission or capability documentation.

## Acceptance Criteria

- All listed files exist only in their new subdirectory locations in the source tree.
- `test/CMakeLists.txt` copies all moved PDS4 files to the correct build-tree subdirectories.
- `test/Makefile.am` copies all moved PDS4 files to the correct build-tree subdirectories and lists them in `EXTRA_DIST`.
- `test/tst_pds4_udf.c` opens every moved label using the updated path constants.
- `tst_pds4_udf` passes with PDS4 enabled under both CMake and Autotools out-of-tree builds.
- `docs/pds4.md` mentions the new `maven/`, `perseverance/`, and `general/` layout.
- No source code or reader behavior changes are introduced.

## Testing Requirements

- Run `tst_pds4_udf` with PDS4 enabled under CMake.
- Run `tst_pds4_udf` with PDS4 enabled under Autotools.
- Verify no stale top-level PDS4 files remain in the build tree after a clean build.

## Dependencies

- None beyond the existing PDS4 build configuration.

## Definition of Done

Files are reorganized, build systems and tests are updated, all PDS4 tests pass, and documentation reflects the new layout.
